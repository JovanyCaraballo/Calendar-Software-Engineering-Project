from PyQt5.QtWidgets import *
from datetime import datetime
from day import Day
import calendar
import random
import sys

class Month:
    def __init__(self, parent, month_num):
        self.month_num = month_num
        self.num_of_days = 0
        self.month = []
        self.i = 0
        self.scan_days(parent)

    def scan_days(self, parent):
        current_date = datetime.now()
        print(calendar.monthrange(current_date.year, self.month_num))
        self.num_of_days = calendar.monthrange(current_date.year, self.month_num)
        self.num_of_days = self.num_of_days[1]
        print(self.num_of_days)

        while len(self.month) < self.num_of_days: # adjusts based on month
            print(self.i)
            self.month.append(self.i)
            self.i = self.i + 1
            dialog = Day(parent)               


            # self.month.append(Day()) # creates instance of days

# def main():
#     app = QApplication(sys.argv)
#     gui = Month(random.randint(1,12)) # check range
#     #gui.show()
#     sys.exit(app.exec_())
#
#
# main()
